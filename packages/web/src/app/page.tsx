export default function HomePage() {
  return (
    <main style={{ padding: '2rem', maxWidth: '60rem', margin: '0 auto' }}>
      <h1>Expense Tracker</h1>
      <p>
        Dashboard, ledger and currency converter screens land in subsequent commits. The Next.js app
        is wired up to MSW so the UI can be built and demoed before any backend endpoint exists.
      </p>
    </main>
  );
}
