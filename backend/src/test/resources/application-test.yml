spring:
  flyway:
    enabled: true
  lifecycle:
    timeout-per-shutdown-phase: 0s

server:
  shutdown: immediate

outbox:
  relay:
    enabled: false

treasury:
  retry:
    delay: 0

logging:
  level:
    root: WARN
    com.example.expensechallenge: INFO
    org.springframework.web: WARN
